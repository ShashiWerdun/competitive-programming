//INCOMPLETE
#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ull unsigned long long

int main()
{
    int n, m;
    ull size = pow(10, 9) + 1;
    cin >> n >> m;
    ull a[n];
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
}